apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: hydrus
data:
  APP_ENV: "production"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "hydrus"
  CORS_ORIGINS: "https://hydrus.example.com"
